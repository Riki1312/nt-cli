# 2026-05-06

## nt-cli refinements

- Fixed stale docs that described database queries as `--filter` JSON; docs now match the implemented SQL query interface.
- Added golden tests for the main response transformers under `internal/transform/testdata/`.
- Added structured error classification for MCP/tool errors: not found, auth, rate limited, and permission failures now map to the documented JSON codes and exit codes.
- Added a private CLI `app` dependency seam for command-level tests without real auth/network calls.
- Added command-level tests for database SQL request shaping and page replace read-then-write behavior.
- Removed the overlapping `replacePageContent` unit test in favor of the command-level page replace test.
- Recorded testing guidance in `context/decisions.md`: prefer high-value integration/e2e tests, use unit tests when command/e2e coverage is difficult, and treat line count as a resource.
- Fixed README and design-doc drift around SQL bulk-update examples, login printing an authorization URL, create returning an array, and `--raw` returning an MCP tool result rather than a full JSON-RPC envelope.
- Updated `Makefile` so `make test` and `make lint` use `.cache/go-build`, keeping verification runnable in the project workspace.
- Added focused OAuth helper tests for URL construction and refresh-token fallback.
- Changed create/duplicate transform results to use an explicit `CreatedPage` type instead of `any`.
- Tightened resource command arity so extra positional arguments fail before auth/network calls.
- Verified with `GOCACHE=/tmp/nt-cli-go-build go test ./...`.
- Verified with `make test` and `make lint` after adding the repo-local Go build cache.
